# Project 4 — Measurement Log

Regenerate with `uv run python run_suite.py --model qwen3.5:4b` and `uv run python run_suite.py --model qwen3.5:9b` (each needs Ollama reachable with that model pulled), or `uv run python render_results.py` to rebuild this file from whichever `measurements/runs_*.jsonl` already exist without re-running the suite. Per-task detail (including the evaluator's specific failure feedback, not just pass/fail) lives in those jsonl files. Validated with synthetic jsonl to confirm the renderer itself is correct; no real trials recorded yet.

## 4B run — 20 tasks

<!-- MEASURE:BEGIN run-4b -->
No runs recorded yet for `qwen3.5:4b`. Run `uv run python run_suite.py --model qwen3.5:4b`.
<!-- MEASURE:END run-4b -->

## 9B run — 20 tasks (same task set)

<!-- MEASURE:BEGIN run-9b -->
No runs recorded yet for `qwen3.5:9b`. Run `uv run python run_suite.py --model qwen3.5:9b`.
<!-- MEASURE:END run-9b -->

## Takeaway

<!-- MEASURE:BEGIN takeaway -->
<!-- Fill in once both the 4B and 9B runs exist: does the gap narrow on the 9B? By how much? -->
<!-- MEASURE:END takeaway -->
