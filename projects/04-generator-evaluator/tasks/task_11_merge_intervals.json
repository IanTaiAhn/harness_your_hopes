{
  "id": "task_11_merge_intervals",
  "prompt": "Write a Python file at tasks/solutions/task_11_merge_intervals.py that defines `merge_intervals(intervals: list[list[int]]) -> list[list[int]]`. Merge all overlapping intervals and return the merged list sorted by start. Two intervals that only touch at an endpoint (e.g. [1, 3] and [3, 5]) count as overlapping and must be merged into one.",
  "test_file": "test_task_11.py"
}
